<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wizut.bukmacher">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WDLogin"
            android:theme="@style/WDTheme.NoActionBar" />
        <activity
            android:name=".WDNavigationDrawer"
            android:label="@string/title_activity_wdnavigation_drawer"
            android:theme="@style/WDTheme.NoActionBar"></activity>
    </application>

</manifest>